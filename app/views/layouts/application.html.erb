<!DOCTYPE html>
<html>
  <head>
    <title>From The Ground Up :: A Community of Learners</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
    <%= favicon_link_tag %>
  </head>
    <body class="<%= controller_name %> <%= action_name %> <%= @plan && @plan.user ? current_user && @plan.user.id == current_user.id ? "correct_user" : "visiting_user" : "visiting_user" %>">
    <div class="<%= "wrapper" if current_page?(root_url) %>">
        <%= render 'shared/navbar' %>
        <%= render 'shared/flashes' %>
        <%= yield %>
    </div>
  </body>
</html>
